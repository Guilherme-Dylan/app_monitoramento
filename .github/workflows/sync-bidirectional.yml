name: Legacy Bidirectional Sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: |
          echo "This legacy workflow is intentionally disabled."
          echo "Production sync is handled by .github/workflows/deploy.yml."
